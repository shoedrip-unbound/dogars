import * as mocha from 'mocha';
import * as chai from 'chai';

const expect = chai.expect;

describe('Dogars Database tests', () => {
    beforeEach(() => {
    });

    it('Check if champ creation works', async () => {
        expect(1).equals(1);
    });

    it('Check if champ deletion works', async () => {
        expect(1).equals(1);
    });

    
});