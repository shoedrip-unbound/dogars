import * as mocha from 'mocha';
import * as chai from 'chai';

import * as fchan from './fchan';

const expect = chai.expect;

describe('fchan tests', () => {
    beforeEach(() => {
    });

    it('should do something', () => {
        expect(3 + 4).to.equal(7);
    });

});